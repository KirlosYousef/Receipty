services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DB_PATH: /app/data/receipts.db
      COST_LOG_PATH: /app/logs/cost.jsonl
    volumes:
      - receipty_data:/app/data
      - ./logs:/app/logs

volumes:
  receipty_data:
